import React, { ReactElement } from 'react';
import { display } from './style.module.scss';

export default function Display():ReactElement {
  return (
    <div className={display}>1</div>  
  );
};
